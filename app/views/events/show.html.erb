<h2><%= @event.name %></h2>
<h3>Metrics: </h3>
<ul>
<% @event.metrics.each do |m| %>
  <li><%= m.int_val || m.boolean_val || m.time_val %></li>
<% end %>
</ul>

<%= link_to "Edit Metrics", edit_event_path, class: "btn btn-default btn-xs" %>
