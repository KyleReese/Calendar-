<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for @event do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.submit "Create Event", class: "btn btn-primary" %>
    <% end %>
  </div>
